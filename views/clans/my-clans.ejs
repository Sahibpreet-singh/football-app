<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>My Clan</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen">
  <div class="max-w-3xl mx-auto p-6">
    <h1 class="text-3xl font-bold text-green-400 mb-6 text-center">My Clan</h1>

    <% if (!clan) { %>
      <p class="text-center text-red-400">You are not part of any clan yet.</p>
    <% } else { %>
      <div class="bg-gray-800 p-6 rounded-lg shadow-md border border-green-400">
        <p class="mb-2"><strong>Name:</strong> <%= clan.name %></p>
        <p class="mb-2"><strong>Admin:</strong> <%= clan.admin.displayName %></p>
        <p class="mb-4"><strong>Members:</strong></p>
        <ul class="list-disc ml-6 text-green-300">
          <% clan.members.forEach(member => { %>
            <li><%= member.displayName %></li>
          <% }); %>
        </ul>
      </div>
    <% } %>
  </div>
</body>
</html>
