<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login - Football MatchFixer Pro</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white flex items-center justify-center min-h-screen">

  <div class="bg-gray-800 shadow-lg rounded-lg p-10 max-w-md w-full text-center">
    <h1 class="text-3xl font-bold mb-6">Welcome to <span class="text-red-500">MatchFixer Pro</span></h1>
    <p class="mb-6 text-gray-300">Sign in to access your dashboard and fix matches like a pro.</p>

    <% if (user) { %>
      <p class="text-lg mb-4">Hello, <%= user.displayName %> 👋</p>
      <a href="/logout" class="inline-block px-6 py-3 bg-red-600 hover:bg-red-700 rounded text-white font-semibold">
        Logout
      </a>
    <% } else { %>
      <a href="/auth/google" class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-white hover:bg-gray-200 text-black rounded font-semibold">
        <img src="https://www.svgrepo.com/show/355037/google.svg" alt="Google Icon" class="w-5 h-5" />
        Sign in with Google
      </a>
    <% } %>
  </div>

</body>
</html>
